# telemetry builds with `COPY . .` from this directory. Keep local runtime state
# (spool, reporter identity, salt) and build artifacts out of the image.
.git
telemetry
*.test
*.out
state
spool
*.db
.DS_Store
